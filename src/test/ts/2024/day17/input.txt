Register A: 281474976710661
Register B: 0
Register C: 0

Program: 2,4,1,3,7,5,1,5,0,3,4,2,5,5,3,0

2 4: regs[1] = mod(regs[0], 8);
1 3: regs[1] = regs[1] ^ 3;
7 5: regs[2] = Math.floor(regs[0] / Math.pow(2, reg[1]));
1 5: regs[1] = regs[1] ^ 5;
0 3: regs[0] = Math.floor(regs[0] / Math.pow(2, 3));
4 2: regs[1] = regs[1] ^ regs[2];
5 5: out.push(mod(reg[1], 8));
3 0: if (reg[0] != 0) jmp(0); else halt();

/*2 4*/ regs[1] = mod(regs[0], 8);
/*1 3*/ regs[1] = regs[1] ^ 3;
/*7 5*/ regs[2] = Math.floor(regs[0] / Math.pow(2, regs[1]));
/*1 5*/ regs[1] = regs[1] ^ 5;
/*0 3*/ regs[0] = Math.floor(regs[0] / 8);
/*4 2*/ regs[1] = regs[1] ^ regs[2];
/*5 5*/ out.push(mod(regs[1], 8));
/*3 0*/ if (regs[0] == 0) break;

out.push(mod(mod(reg, 8) ^ 3 ^ 5 ^ Math.floor(reg / Math.pow(2, mod(reg, 8) ^ 3)), 8));
reg = Math.floor(reg / 8);
if (reg == 0) break;

mod(mod(reg, 8) ^ Math.floor(reg / Math.pow(2, mod(reg, 8) ^ 3)), 8) ^ 6 = 2
mod(mod(reg, 8) ^ Math.floor(reg / Math.pow(2, mod(reg, 8) ^ 3)), 8) = 2 ^ 6
mod(reg, 8) ^ Math.floor(reg / Math.pow(2, mod(reg, 8) ^ 3)) = k*8 + (2 ^ 6)
reg >> mod(reg, 8) ^ 3 = k*8 + (2 ^ 6 ^ mod(reg, 8))


